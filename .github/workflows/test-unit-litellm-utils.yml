name: "Unit Tests: LiteLLM Utilities"

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  litellm-utils:
    uses: ./.github/workflows/_test-unit-base.yml
    with:
      test-path: "tests/litellm_utils_tests"
      workers: 2
      reruns: 2
