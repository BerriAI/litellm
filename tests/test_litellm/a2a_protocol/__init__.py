"""A2A protocol test package.

Pytest's default import mode requires package-qualified module names to avoid
"import file mismatch" errors when multiple test modules share the same basename
(e.g. multiple `test_cost_calculator.py` files in different subdirectories).
"""
