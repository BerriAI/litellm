// TODO: Port the complete Python Responses models before enabling these streaming contracts
pub struct ResponsesResponse {
    _schema: (),
}

pub struct ResponseOutputItem {
    _schema: (),
}

pub struct ResponsesRequest {
    _schema: (),
}

pub struct ResponsesEvent {
    _schema: (),
}
