[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "litellm-core"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
module-name = "litellm_core"
features = ["pyo3/extension-module"]
