## 直通代理

### 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `POST` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `GET` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `DELETE` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `PUT` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `PATCH` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `POST` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `GET` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `DELETE` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `PUT` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `PATCH` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `POST` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `GET` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `DELETE` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `PUT` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `PATCH` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `POST` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `GET` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `DELETE` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `PUT` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `PATCH` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `POST` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `GET` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `DELETE` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `PUT` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `PATCH` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `POST` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `GET` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `DELETE` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `PUT` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `PATCH` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `POST` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `GET` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `DELETE` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `PUT` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `PATCH` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `POST` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `GET` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `DELETE` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `PUT` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `PATCH` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `POST` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `GET` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `DELETE` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `PUT` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `PATCH` | `/azure/{endpoint}` | Azure Proxy Route |
| `POST` | `/azure/{endpoint}` | Azure Proxy Route |
| `GET` | `/azure/{endpoint}` | Azure Proxy Route |
| `DELETE` | `/azure/{endpoint}` | Azure Proxy Route |
| `PUT` | `/azure/{endpoint}` | Azure Proxy Route |
| `PATCH` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `POST` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `GET` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `DELETE` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `PUT` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `PATCH` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `POST` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `GET` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `DELETE` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `PUT` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `PATCH` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `POST` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `GET` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `DELETE` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `PUT` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `PATCH` | `/openai/{endpoint}` | Openai Proxy Route |
| `POST` | `/openai/{endpoint}` | Openai Proxy Route |
| `GET` | `/openai/{endpoint}` | Openai Proxy Route |
| `DELETE` | `/openai/{endpoint}` | Openai Proxy Route |
| `PUT` | `/openai/{endpoint}` | Openai Proxy Route |
| `PATCH` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `POST` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `GET` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `DELETE` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `PUT` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `PATCH` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `POST` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `GET` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `DELETE` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `PUT` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `PATCH` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `POST` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `GET` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `DELETE` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `PUT` | `/langfuse/{endpoint}` | Langfuse Proxy Route |

#### `PATCH` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Anthropic 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `POST` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `GET` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `DELETE` | `/anthropic/{endpoint}` | Anthropic Proxy Route |
| `PUT` | `/anthropic/{endpoint}` | Anthropic Proxy Route |

#### `PATCH` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /anthropic/{endpoint}

**Anthropic 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/anthropic_completion)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Azure 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/azure/{endpoint}` | Azure Proxy Route |
| `POST` | `/azure/{endpoint}` | Azure Proxy Route |
| `GET` | `/azure/{endpoint}` | Azure Proxy Route |
| `DELETE` | `/azure/{endpoint}` | Azure Proxy Route |
| `PUT` | `/azure/{endpoint}` | Azure Proxy Route |

#### `PATCH` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /azure/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Azure AI 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `POST` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `GET` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `DELETE` | `/azure_ai/{endpoint}` | Azure Proxy Route |
| `PUT` | `/azure_ai/{endpoint}` | Azure Proxy Route |

#### `PATCH` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /azure_ai/{endpoint}

**Azure 直通路由**

通过代理调用任意 Azure 端点。

直接使用 `{PROXY_BASE_URL}/azure/{endpoint:path}` 即可。

检查 URL 中的 deployment id 是否为 LiteLLM 模型名，若是则通过 `llm_router.allm_passthrough_route` 进行路由。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Bedrock 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `POST` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `GET` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `DELETE` | `/bedrock/{endpoint}` | Bedrock Proxy Route |
| `PUT` | `/bedrock/{endpoint}` | Bedrock Proxy Route |

#### `PATCH` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /bedrock/{endpoint}

**Bedrock 直通路由**

Bedrock v1 直通端点。
V2 由 `/bedrock/v2` 端点处理。
[Docs](https://docs.litellm.ai/docs/pass_through/bedrock)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Cohere 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `POST` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `GET` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `DELETE` | `/cohere/{endpoint}` | Cohere Proxy Route |
| `PUT` | `/cohere/{endpoint}` | Cohere Proxy Route |

#### `PATCH` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /cohere/{endpoint}

**Cohere 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/cohere)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Cursor 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `POST` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `GET` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `DELETE` | `/cursor/{endpoint}` | Cursor Proxy Route |
| `PUT` | `/cursor/{endpoint}` | Cursor Proxy Route |

#### `PATCH` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /cursor/{endpoint}

**Cursor 直通路由**

Cursor Cloud Agents API 直通端点。

支持 Cursor Cloud Agents 的全部端点：
- GET    /v0/agents         — List agents
- POST   /v0/agents         — Launch an agent
- GET    /v0/agents/{id}    — Agent status
- GET    /v0/agents/{id}/conversation — Agent conversation
- POST   /v0/agents/{id}/followup    — Add follow-up
- POST   /v0/agents/{id}/stop        — Stop an agent
- DELETE /v0/agents/{id}    — 删除 Agent。
- GET    /v0/me             — API key info
- GET    /v0/mo...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Google AI Studio 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `POST` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `GET` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `DELETE` | `/gemini/{endpoint}` | Gemini Proxy Route |
| `PUT` | `/gemini/{endpoint}` | Gemini Proxy Route |

#### `PATCH` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /gemini/{endpoint}

**Gemini 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/google_ai_studio)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Langfuse 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `POST` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `GET` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `DELETE` | `/langfuse/{endpoint}` | Langfuse Proxy Route |
| `PUT` | `/langfuse/{endpoint}` | Langfuse Proxy Route |

#### `PATCH` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /langfuse/{endpoint}

**Langfuse 直通路由**

通过 LiteLLM 代理调用 Langfuse，兼容 Langfuse SDK。

[Docs](https://docs.litellm.ai/docs/pass_through/langfuse)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Mistral 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `POST` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `GET` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `DELETE` | `/mistral/{endpoint}` | Mistral Proxy Route |
| `PUT` | `/mistral/{endpoint}` | Mistral Proxy Route |

#### `PATCH` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /mistral/{endpoint}

**Mistral 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/mistral)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### OpenAI 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/openai/{endpoint}` | Openai Proxy Route |
| `POST` | `/openai/{endpoint}` | Openai Proxy Route |
| `GET` | `/openai/{endpoint}` | Openai Proxy Route |
| `DELETE` | `/openai/{endpoint}` | Openai Proxy Route |
| `PUT` | `/openai/{endpoint}` | Openai Proxy Route |
| `PATCH` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `POST` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `GET` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `DELETE` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |
| `PUT` | `/openai_passthrough/{endpoint}` | Openai Proxy Route |

#### `PATCH` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /openai/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /openai_passthrough/{endpoint}

**OpenAI 直通路由**

OpenAI API 直通端点。

以下两个路径均可用：
- /openai/{endpoint:path} - Standard OpenAI passthrough route
- /openai_passthrough/{endpoint:path} - Dedicated passthrough route (recommended for Responses API)

当需要无冲突地直通 OpenAI 时，请使用 `/openai_passthrough/*`。
以避免与 LiteLLM 的原生实现冲突（例如 `/v1/responses` 的 Responses API）。

示例:
    Standard route:
    - /openai/v1/chat/completions
    - /openai/v1/assista...

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Vertex AI 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `POST` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `GET` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `DELETE` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `PUT` | `/vertex_ai/discovery/{endpoint}` | Vertex Discovery Proxy Route |
| `PATCH` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `POST` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `GET` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `DELETE` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |
| `PUT` | `/vertex_ai/{endpoint}` | Vertex Proxy Route |

#### `PATCH` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /vertex_ai/discovery/{endpoint}

**Vertex Discovery 直通路由**

通过代理调用任意 Vertex Discovery 端点。

直接使用 `{PROXY_BASE_URL}/vertex_ai/discovery/{endpoint:path}` 即可。

目标 URL：`https://discoveryengine.googleapis.com`。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PATCH` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /vertex_ai/{endpoint}

**Vertex 直通路由**

通过 Vertex AI SDK 调用 LiteLLM 代理。

[Docs](https://docs.litellm.ai/docs/pass_through/vertex_ai)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### VLLM 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `POST` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `GET` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `DELETE` | `/vllm/{endpoint}` | Vllm Proxy Route |
| `PUT` | `/vllm/{endpoint}` | Vllm Proxy Route |

#### `PATCH` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /vllm/{endpoint}

**vLLM 直通路由**

[Docs](https://docs.litellm.ai/docs/pass_through/vllm)

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### AssemblyAI 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `POST` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `GET` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `DELETE` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `PUT` | `/assemblyai/{endpoint}` | Assemblyai Proxy Route |

#### `PATCH` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### AssemblyAI EU 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `POST` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `GET` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `DELETE` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |
| `PUT` | `/eu.assemblyai/{endpoint}` | Assemblyai Proxy Route |

#### `PATCH` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /eu.assemblyai/{endpoint}

**AssemblyAI 直通路由**

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

### Milvus 直通代理

| 方法 | 路径 | 说明 |
|------|------|------|
| `PATCH` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `POST` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `GET` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `DELETE` | `/milvus/{endpoint}` | Milvus Proxy Route |
| `PUT` | `/milvus/{endpoint}` | Milvus Proxy Route |

#### `PATCH` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `POST` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `GET` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `DELETE` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

#### `PUT` /milvus/{endpoint}

**Milvus 直通路由**

启用 Milvus `/vectors` 端点作为直通端点。

**参数：**

| 参数名 | 位置 | 必填 | 说明 |
|--------|------|------|------|
| `endpoint` | 路径参数 | ✅ |  |

