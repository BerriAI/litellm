# Proof media for PR #33430 independent e2e verification

This orphan branch hosts static proof media referenced from the description of BerriAI/litellm PR #33430 (per-component PodDisruptionBudget and topologySpreadConstraints in the componentized helm chart)

Files under `proof/` are rendered inline in the PR description and contain no application code
