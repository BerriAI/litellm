[pytest]
addopts = -n auto --cov=litellm --cov-report=term-missing --cov-report=html
testpaths = tests
python_files = test_*.py 

# 设置测试超时时间
timeout = 300

# 显示详细的测试输出
verbose = true

# 忽略特定的警告
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning