# Batch 06 — Analytics / spend / failure logs

**Commits:** 13 (includes one revert pair — candidate for DROP-both)
**Scope:** Active-user analytics + DAU/WAU/MAU endpoints, user leaderboard, team-based filtering, aggregated failure-logs dashboard, spend-logs analytics section, schema migration for aggregated spend logs (reverted).

## Upstream landscape (v1.81.3 → v1.83.3)

| File | # upstream commits | Notes |
|---|---|---|
| `user_agent_analytics_endpoints.py` | 1 | Our file; single upstream edit |
| `spend_management_endpoints.py` | 18 | Moderate |
| `auth_exception_handler.py` | 0 | Untouched upstream |
| `db_spend_update_writer.py` | 19 | Moderate |
| `proxy/utils.py` | 62 | High |
| `ui/user_agent_activity.tsx` | 0 | Ours only |
| `ui/networking.tsx` | 93 | **Very high** — our centralized networking file also heavily modified upstream |
| `ui/ErrorStatsTable.tsx` | 0 | Ours only |
| `ui/view_logs/index.tsx` | 31 | Moderate |
| `ui/new_usage.tsx` | 0 | Ours only |

### Upstream equivalents

- **DAU/WAU/MAU analytics**: zero upstream matches. Our endpoints are uniquely ours.
- **Aggregated failure logs**: zero upstream matches. `ErrorStatsTable.tsx` and related dashboard do not exist upstream.
- **Spend-log aggregation**: `9c3fab24ad fix(proxy): restore per-entity breakdown in aggregated daily activity endpoint` — related but different (daily activity endpoint, not our spend-logs aggregation).

### Drop-gate verdicts

| Custom feature | Upstream equivalent? | Safe to DROP? |
|---|---|---|
| Active-user analytics + DAU/WAU/MAU | No | No |
| Aggregated failure logs dashboard | No | No |
| Spend-logs analytics section | No | No |
| Schema migration pair (#111/#115) | Net zero (reverted in place) | **YES — DROP both** |

---

## Per-commit audit

### a834c424aa, eea3e7c959, 108102c4d0, 2b0d85a334, de3e3148ed — active-user analytics family (#26, #30, #32, #35, #40)

- **files:** `user_agent_analytics_endpoints.py`, `networking.tsx`, `user_agent_activity.tsx`, `new_usage.tsx`, test file
- **decision:** **REWORK**
- **rationale:** `networking.tsx` has 93 upstream commits — our API method additions will land amid significant upstream edits. `user_agent_analytics_endpoints.py` has only 1 upstream commit; our endpoint additions mostly clean. UI-only files (user_agent_activity, new_usage) are ours alone → clean.
- **replay plan:**
  1. Cherry-pick each commit; for `networking.tsx`, merge our `fetch*` helpers alongside upstream's new ones.
  2. `user_agent_analytics_endpoints.py` likely clean.
  3. UI components apply clean.
- **verification:** MUST-SURVIVE item #11 (DAU/WAU/MAU + leaderboard).

### bcc40ce563 — failure-logs aggregated dashboard (#47)

- **files:** `spend_management_endpoints.py` (18 upstream), `networking.tsx` (93), `ErrorStatsTable.tsx` (new — ours), `view_logs/index.tsx` (31)
- **decision:** **REWORK**
- **rationale:** Spans backend + UI-networking + new UI component + existing tab list.
- **replay plan:** New component applies clean. Backend endpoint additions need manual merge into `spend_management_endpoints.py`. `view_logs/index.tsx` tab-registration is a known conflict surface — add our tab alongside upstream's.
- **verification:** MUST-SURVIVE item #12 (failure-logs dashboard).

### 78b5c0453f — spend-logs analytics section (#53)

- **files:** `spend_management_endpoints.py` (18), `package.json` + `package-lock.json`, `networking.tsx` (93), `ErrorStatsTable.tsx`, `view_logs/index.tsx` (31)
- **decision:** **REWORK**
- **rationale:** Package-lock regeneration will cause large but mechanical diffs — resolve by regenerating after merge. Same pattern as #47 for endpoint + UI.
- **replay plan:**
  1. Resolve `package.json` intent (our added deps).
  2. Regenerate `package-lock.json` via `npm install` post-cherry-pick rather than resolving conflict manually.
  3. Resolve endpoint + UI files as in #47.
- **verification:** MUST-SURVIVE item #13 (spend-logs analytics toggle + time range + team filter).

### 5d4a66f142 — adding back failure-logs aggregation + local env fixes (#87)

- **files:** `docker-compose.yml`, `litellm/proxy/_experimental/out/**` (Next.js build artifacts)
- **decision:** **REWORK — drop the build-artifact hunks**
- **rationale:** `_experimental/out/**` files are generated Next.js build output. They should NOT be carried forward — they'll be regenerated by the UI build post-upgrade.
- **replay plan:**
  1. Cherry-pick with `-n` flag (no-commit); interactively de-stage all `_experimental/out/**` paths before committing.
  2. Keep only `docker-compose.yml` changes (and any actual source-code changes if any).
  3. Post-merge, re-run `npm run build` to regenerate build artifacts.
- **verification:** MUST-SURVIVE item #12.

### 61fb9f323f — remove duplicate code (#89)

- **files:** `db_spend_update_writer.py` (19 upstream, 82-line deletion)
- **decision:** **CONDITIONAL REWORK — may become no-op**
- **rationale:** Removes 82 lines of duplicated spend-update code. If upstream's refactors to `db_spend_update_writer.py` have already cleaned the duplication, our fix may be no-op.
- **replay plan:** Check post-batch-04/05 state. If the duplicate lines no longer exist, skip with rationale "upstream refactor removed the duplication".
- **verification:** n/a (code hygiene).

### ef5a4e8ccf — metadata shown for failure logs (#104)

- **files:** `proxy/utils.py` (62 upstream)
- **decision:** **REWORK**
- **rationale:** Small addition in a high-churn file. Need to relocate to the current utils layout.

### 85019edec0 — Fix/failure log (#107)

- **files:** `auth/auth_exception_handler.py` (0 upstream)
- **decision:** **KEEP-AS-IS**
- **rationale:** Untouched upstream.

### 20caa0aebb — schema migration for aggregated spend logs (#111)

- **files:** `Dockerfile`, migration.sql, schema.prisma files, requirements.txt
- **decision:** **DROP (paired with #115)**

### 596a3a3a5a — Revert "schema migration for aggregated spend logs" (#115)

- **files:** same as #111 (exact inverse diff)
- **decision:** **DROP (paired with #111)**

#### Drop-gate check for #111 + #115

1. Behavior equivalence: net diff of pair = empty (revert cleanly undoes feature). ✓
2. No semantic regression: applying neither means no schema change — matches target state. ✓
3. Verification test: feature was never released in prod, no user-facing behavior to verify. ✓
4. Human sign-off: **requires reviewer (TBD) to confirm the revert is truly in-place and no partial downstream changes depend on #111's schema.**

Conditional on #4. Recommended action: before Phase 3, run `git diff 20caa0aebb^ 596a3a3a5a` — if empty, DROP both unconditionally.

---

## Batch summary

| # | SHA | Decision | Risk |
|---|---|---|---|
| 1 | a834c424aa | REWORK | MED |
| 2 | eea3e7c959 | REWORK | MED |
| 3 | 108102c4d0 | REWORK | LOW |
| 4 | 2b0d85a334 | REWORK | MED |
| 5 | de3e3148ed | REWORK | MED |
| 6 | bcc40ce563 | REWORK | HIGH |
| 7 | 78b5c0453f | REWORK | HIGH |
| 8 | 5d4a66f142 | REWORK (strip build artifacts) | MED |
| 9 | 61fb9f323f | Conditional drop | LOW |
| 10 | ef5a4e8ccf | REWORK | LOW |
| 11 | 85019edec0 | KEEP-AS-IS | LOW |
| 12 | 20caa0aebb | **DROP** (pair with 596a3a3a5a) | n/a |
| 13 | 596a3a3a5a | **DROP** (pair with 20caa0aebb) | n/a |

**Two DROPs** (both halves of revert pair #111/#115), **one conditional DROP** (#89 if upstream refactor makes it no-op). All other custom analytics patches are uniquely needed — upstream has no equivalents for DAU/WAU/MAU, aggregated failure logs, or our spend-logs analytics section.

## Replay notes

- For #5d4a66f142 (#87), be aggressive about NOT bringing in `_experimental/out/**` build artifacts. Clean those hunks before committing.
- For #78b5c0453f (#53), regenerate `package-lock.json` via `npm install` rather than resolving the conflict manually.
- Confirm the #111/#115 drop with a pre-Phase-3 command: `git diff 20caa0aebb^ 596a3a3a5a | wc -l` — expect 0.
