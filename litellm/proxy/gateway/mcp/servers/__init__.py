"""servers — MCP Gateway v2

S2/S14 · what upstreams exist (source of truth). Frozen MCPServerSpec (config+DB merged once), ServerRegistry atomic-swap + shared resolver, toolsets/virtual servers.
"""
