"""Built-in callbacks ported to the v1 contract.

Nothing here is registered by importing it, and nothing in the product imports it yet:
a port is exercised by its tests only, until its legacy twin is removed. `AGENTS.md`
beside this file is the porting convention, `manifest.py` is the list of ports.
"""
