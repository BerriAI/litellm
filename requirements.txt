# LITELLM PROXY DEPENDENCIES #
anyio==4.4.0 # openai + http req.
openai==1.45.0 # openai req. 
fastapi==0.114.1 # server dep
backoff==2.2.1 # server dep
pyyaml==6.0.2 # server dep
uvicorn==0.30.6 # server dep
gunicorn==23.0.0 # server dep
boto3==1.34.34 # aws bedrock/sagemaker calls
redis==5.0.8 # caching
numpy==1.26.4 # semantic caching
prisma==0.15.0 # for db
mangum==0.17.0 # for aws lambda functions
pynacl==1.5.0 # for encrypting keys
google-cloud-aiplatform==1.66.0 # for vertex ai calls
anthropic[vertex]==0.34.2
google-generativeai==0.8.1 # for vertex ai calls
async-generator==1.10 # for async ollama calls
langfuse==2.48.0 # for langfuse self-hosted logging
datadog-api-client==2.28.0 # for datadog logging
prometheus-client==0.20.0 # for /metrics endpoint on proxy
orjson==3.10.7 # fast /embedding responses
apscheduler==3.10.4 # for resetting budget in background 
fastapi-sso==0.15.0 # admin UI, SSO
pyjwt[crypto]==2.9.0
python-multipart==0.0.9 # admin UI
pillow==10.4.0
azure-ai-contentsafety==1.0.0 # for azure content safety
azure-identity==1.17.1 # for azure content safety
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
detect-secrets==1.5.0 # Enterprise - secret detection / masking in LLM requests
cryptography==43.0.1

### LITELLM PACKAGE DEPENDENCIES
python-dotenv==1.0.1 # for env 
tiktoken==0.7.0 # for calculating usage
importlib-metadata==8.4.0 # for random utils
tokenizers==0.20.0 # for calculating usage
click==8.1.7 # for proxy cli 
jinja2==3.1.4 # for prompt templates
certifi==2024.8.30 # [TODO] clean up 
aiohttp==3.10.2 # for network calls
aioboto3==12.3.0 # for async sagemaker calls
tenacity==9.0.0  # for retrying requests, when litellm.num_retries set
pydantic[email]==2.9.1 # proxy + openai req.
jsonschema==4.23.0 # validating json schema
