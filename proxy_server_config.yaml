model_list:
  - model_name: gpt-3.5-turbo-end-user-test
    litellm_params:
      model: gpt-3.5-turbo
      region_name: "eu"
    model_info:
      id: "1"
      access_groups: ["default-models"] # 👈 Model Access Group
  
general_settings: 
  master_key: sk-1234 # [OPTIONAL] Use to enforce auth on proxy. See - https://docs.litellm.ai/docs/proxy/virtual_keys
  # store_model_in_db: True
  port: 4000 # Server port - always use port 4000

  database_url: "postgresql://teddyamkie@localhost:5432/litellm_db" # [OPTIONAL] use for token-based auth to proxy

